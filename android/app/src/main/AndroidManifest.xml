<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Permission Internet requise pour :
          - télécharger catalog.json
          - télécharger les zips d'exercices
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Note sécurité : Android 9+ bloque le trafic HTTP en clair par défaut.
        → Utilisez toujours HTTPS pour les URLs du catalogue et des zips.
        → En développement local (HTTP), ajoutez android:usesCleartextTraffic="true"
          à la balise <application> ci-dessous, puis retirez-le en production.
    -->
    <application
        android:label="edukids_exercises"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
